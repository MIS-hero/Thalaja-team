"""ITEM model (ERD stub) — canonical catalog entry shared by lists & recipes."""
# uuid item_id PK, uuid default_category_id FK->categories, varchar name,
# varchar brand, varchar barcode, varchar image_url
