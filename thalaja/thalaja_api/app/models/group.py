"""GROUP model (ERD stub) — type: individual | household."""
# uuid group_id PK, uuid created_by FK->users, name, image_url,
# enum type(individual|household), varchar invite_code UNIQUE,
# created_at, updated_at
