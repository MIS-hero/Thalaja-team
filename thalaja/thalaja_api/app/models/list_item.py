"""LIST_ITEM model (ERD stub) — one entry per item added to a list."""
# uuid list_item_id PK, uuid list_id FK, uuid item_id FK, uuid added_by FK,
# uuid bought_by FK, float quantity, varchar unit, bool is_bought,
# bool is_urgent, varchar image_url, text notes, created_at, updated_at
