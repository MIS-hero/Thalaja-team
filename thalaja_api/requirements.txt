# ── Core Framework ──
Flask==3.0.0
flask-restx==1.3.0
Werkzeug==3.0.1

# ── Database (SQLAlchemy + PostgreSQL for Supabase) ──
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.23
psycopg2-binary==2.9.9

# ── Migrations ──
Flask-Migrate==4.0.5

# ── Authentication & Security ──
PyJWT==2.8.0
cryptography==41.0.7

# ── HTTP Client (for Supabase REST API calls) ──
requests==2.31.0

# ── Environment Variables ──
python-dotenv==1.0.0

# ── CORS ──
Flask-Cors==4.0.0

# ── Testing ──
pytest==7.4.3
pytest-cov==4.1.0
